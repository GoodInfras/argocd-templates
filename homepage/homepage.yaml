apps_values:
  homepage:
    targetRevision: '">0.0.0"'
    domain: .kind
apps_dict_tpl:
  homepage:
    homepage:
      spec:
        source:
          repoURL: 'https://jameswynn.github.io/helm-charts'
          chart: homepage
          targetRevision: '{{  index .Values.apps_values "homepage" "targetRevision" }}'
          helm:
            valueFiles:
              - values.yaml 
            valuesObject:      
              host: &host homepage{{  index .Values.apps_values "homepage" "domain" }}
              env:
                - name: HOMEPAGE_ALLOWED_HOSTS
                  value: *host              
              enableRbac: true
              serviceAccount:
                create: true
              config:
                settings:
                  quicklaunch:
                    searchDescriptions: true
                    hideInternetSearch: true
                    showSearchSuggestions: true
                    hideVisitURL: true
                    provider: google                       
                  headerStyle: boxedWidgets
                services: {}
                widgets:                          
                  - resources:
                      # change backend to 'kubernetes' to use Kubernetes integration. Requires RBAC.
                      backend: kubernetes
                      expanded: true
                      cpu: true
                      memory: true
                  - search:
                      target: _blank
                      quicklaunch:
                        searchDescriptions: true
                        hideInternetSearch: true
                        showSearchSuggestions: true
                        hideVisitURL: true
                      provider: google                                 
                  - kubernetes:
                      cluster:
                        show: true
                        cpu: true
                        memory: true
                        showLabel: true
                        label: "cluster"
                      nodes:
                        show: true
                        cpu: true
                        memory: true
                        showLabel: true   
                kubernetes:
                  mode: cluster                           
              ingress:
                main:
                  enabled: true
                  labels:
                    gethomepage.dev/enabled: "true"
                  annotations:
                    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
                    gethomepage.dev/name: "homepage"
                    gethomepage.dev/description: "A modern, secure, highly customizable application dashboard."
                  ingressClassName: "nginx"
                  hosts:
                    - host: *host
                      paths:
                        - path: /
                          pathType: Prefix
